# 建置的除錯版本（由 README 的手動指令串接產生，build.sh 不會產生它，不需版控）
dist/debug.html

# 注意：dist/index.html 要進版控——它是可直接下載使用的單檔成品，
# GitHub Pages 也由 build.sh 重新產生後部署。不要把 dist/ 整個忽略。

# Streamlit
.streamlit/secrets.toml

# Python
__pycache__/
*.py[cod]
.venv/
venv/
env/

# 作業系統與編輯器
.DS_Store
Thumbs.db
desktop.ini
*.swp
.vscode/
.idea/

# 本機暫存
*.log
node_modules/
